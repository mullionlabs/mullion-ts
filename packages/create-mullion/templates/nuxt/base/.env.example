# {{PROJECT_NAME}} Environment Configuration
#
# Copy this file to .env and fill in your API keys.
# The app works without API keys (mock mode) for testing.

# =============================================================================
# LLM Provider API Keys (optional - enables real LLM calls)
# =============================================================================

# OpenAI API Key (https://platform.openai.com/api-keys)
# NUXT_OPENAI_API_KEY=sk-...

# Anthropic API Key (https://console.anthropic.com/settings/keys)
# NUXT_ANTHROPIC_API_KEY=sk-ant-...

# OpenAI structured outputs strict mode (optional)
# Set to false if your schemas include optional fields
# NUXT_OPENAI_STRICT_JSON_SCHEMA=false

# =============================================================================
# Notes
# =============================================================================
# - Without API keys, the app runs in "mock mode" with simulated responses
# - Only one provider key is needed for real LLM calls
# - Anthropic is checked first, then OpenAI
