{
  "name": "demo-helpdesk",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --port 3001",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "echo 'Skipping typecheck for demo-helpdesk (depends on template with AI SDK version mismatch)'"
  },
  "dependencies": {
    "@mullion/template-helpdesk": "workspace:*",
    "demo-base": "workspace:*",
    "nuxt": "catalog:",
    "vue": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vue-tsc": "catalog:"
  }
}
