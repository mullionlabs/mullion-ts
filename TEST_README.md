# Mullion: Type-Safe LLM Context Management

![Mullion Banner](https://via.placeholder.com/1200x300.png?text=Mullion:+Type-Safe+LLM+Context+Management&bg=18181b&fg=ffffff)

Унеможливте витоки контексту "за дизайном". Шар безпеки та типізації для надійних, масштабованих та аудийованих
LLM-процесів на TypeScript.

---

## ⚠️ Проблема: Криза "Витоку контексту" (Context Leak)

У сучасній AI-інженерії контекстне вікно — це оперативна пам'ять вашого додатку. Але управління цією пам'яттю є крихким.
Одна помилка в коді може призвести до того, що конфіденційні дані адміністратора будуть передані у відповідь для
публічного користувача.

Традиційні фреймворки обробляють контекст неявно, передаючи глобальні об'єкти стану, які безконтрольно зростають і
легко "протікають".

| Підхід               | Стан          | Опис                                             |
| :------------------- | :------------ | :----------------------------------------------- |
| **Неявний контекст** | ❌ Небезпечно | Глобальні об'єкти, відсутність кордонів доступу. |
| **Mullion Scopes**   | ✅ Безпечно   | Ізольовані області з явною передачею даних.      |

Mullion вирішує це, вводячи концепцію **Scoped Execution** (Виконання в межах області). Дані, створені в одній області (
наприклад, `admin`), не можуть бути використані в іншій (наприклад, `public`) без явного, аудийованого "моста" (
`bridge`), що перевіряється на етапі компіляції.

## ✨ Ціннісна пропозиція

Mullion — це ESLint + TypeScript для LLM-процесів. Ми не є рушієм оркестрації, ми — рівень безпеки над **Vercel AI SDK
**.

- 🛡️ **Безпека через дизайн**: Виявляйте порушення ізоляції тенантів на етапі компіляції.
- ⚡ **Нульові витрати в рантаймі**: Скоупи — це "фантомні типи", що зникають після компіляції.
- 🔍 **Повне походження даних**: Кожне значення несе в собі `traceId` та історію переходів.
- 💰 **Оптимізація кешування**: Інтелектуальні примітиви для Prompt Caching (до 90% економії).

## 🚀 Quick Start

1. **Встановлення**:
   Mullion є модульним. Встановіть ядро та плагін ESLint:

   ```bash
   pnpm add @mullion/core @mullion/ai-sdk
   pnpm add -D @mullion/eslint-plugin
   ```

2. **Конфігурація**:
   Додайте плагін до вашого `eslint.config.mjs` для активації детекції витоків у реальному часі.

3. **Базове використання**:
   Використовуйте `client.scope` для створення захищених меж виконання.

## 🏗️ Архітектура та Концепції

Mullion впроваджує мінімальний набір примітивів:

- **Scope (`client.scope`)**: Кордон, що представляє домен довіри (наприклад, `tenant-123`).
- **Owned Value (`Owned<T, S>`)**: Обгортка навколо даних, прив'язана до скоупа `S`.
- **Bridge (`ctx.bridge`)**: Єдиний безпечний спосіб перемістити дані між скоупами з логуванням.

## 📦 Структура пакетів (Monorepo)

| Пакет                    | Опис                                                | Статус    |
| :----------------------- | :-------------------------------------------------- | :-------- |
| `@mullion/core`          | Примітиви: Scopes, Owned, Fork, Merge.              | ✅ Stable |
| `@mullion/ai-sdk`        | Інтеграція з Vercel AI SDK та логіка кешування.     | ✅ Stable |
| `@mullion/eslint-plugin` | Правила статичного аналізу для запобігання витокам. | ✅ Stable |

## 🗺️ Дорожня карта (Roadmap)

- [x] **v0.1 (Реліз)**: Базова система Scope, ESLint плагін, Fork & Merge стратегії.
- [ ] **v0.2**: Оцінка вартості (Cost Estimation) та інтеграція з Next.js App Router.
- [ ] **v0.3**: Спостережуваність (OTel) та VSCode Extension для візуалізації скоупів.

## 🤝 Участь у розробці

Ми використовуємо **pnpm workspaces** та **Turborepo**.

```bash
git clone https://github.com/mullionlabs/mullion-ts
pnpm install
pnpm build
pnpm test
```

Детальніше у [CONTRIBUTING.md](./CONTRIBUTING.md).

---

**Ліцензія**: MIT © Mullion Labs
